__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.env.*
.pytest_cache/
.DS_Store

# Internal planning docs — kept local, not published to GitHub.
PLAN.md
ARCHITECTURE_PLAN.md

# rights hygiene — script containers and scratch output never commit.
# Original .fountain fixtures are the only screenplay files allowed in git;
# scripts/rights_guard.py (pre-commit + CI) enforces the same rules.
out/
*.fdx
*.fdr
*.pdf
*.doc
*.docx
*.rtf
*.celtx
*.highland
*.fadein
*.fountain
!fixtures/**/*.fountain

# Claude Code agent worktrees — never commit
.claude/worktrees/
.gstack/
.claude/launch.json
