# Triage workbench (ui/) — kept separate from the root requirements.txt so the
# pipeline's dependency surface stays untouched. psycopg is already a root dep;
# these three add only a small server-rendered web layer (no SPA, no build step).
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1

# Auth (P3-IDENTITY): verify Supabase-minted HS256 access tokens (ui/auth.py).
# Verification only — we never mint tokens.
PyJWT>=2.8
